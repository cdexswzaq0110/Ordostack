# Deployment

Current deployment support is local Docker Compose only.

Environment configuration is documented in [environment.md](environment.md). AWS EC2 or other hosted deployment is planned for a later phase and must not reuse local-only settings such as empty MySQL passwords.
